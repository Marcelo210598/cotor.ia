# Snapshot - 2026-09-15

## 🎯 O que foi feito hoje
- **Fechei a Galeria (Fase 7):** 45/45 prompts. Faltavam 6 (5 AGENT + "Parceiro de
  brainstorm") travados há 3 tentativas por rate limit diário (TPD) do Groq, compartilhado
  com produção. Fix: `analyzeIntent`/`scorePrompt` ganharam escape hatch via env var
  (`ANALYZE_PROVIDER=haiku` / `SCORE_PROVIDER=haiku`) pra rodar o gap-fill sem depender da
  cota do Groq. Testado na UI (fork "Usar como base" funcionando), commitado, deploy feito.
- **Auditoria de segurança completa**, pedida pelo Marcelo em duas rodadas:
  1. Leitura manual guiada por OWASP (skill `owasp-security-check`) nas 14 rotas de API +
     auth + webhooks + headers + dependências. Achei e corrigi 6 problemas reais — o mais
     sério: `/api/cotor` sem rate limit no passo de `analyzeIntent`, dava pra automatizar
     chamada infinita e reabrir o mesmo problema de cota do Groq que travou a Galeria.
  2. O Marcelo questionou se a varredura manual não era rasa demais — busquei e instalei a
     skill `trailofbits/skills@semgrep` (Semgrep CLI real, determinístico) + ele criou conta
     Semgrep grátis e habilitou o **Pro** (cross-file taint tracking). Rodei scan completo:
     **0 findings** em 124 arquivos, confirmando os fixes da rodada manual.
- Instalei globalmente as skills `owasp-security-check` e `trailofbits/skills@semgrep`
  (ficam disponíveis pra outros projetos também).

## 📝 Decisões técnicas importantes
- **CSP com `'unsafe-inline'` em `script-src` é decisão consciente, não pendência**: fechar
  via nonce exigiria renderização dinâmica em toda página (Next.js), matando a
  estática/ISR da landing/SEO (Fase 6). Marcelo confirmou manter como está — documentado
  no `next.config.ts`.
- CVE do `deepmerge-ts` (via Prisma CLI) corrigido com `overrides` no `package.json` forçando
  `^8.0.2`, sem migrar o Prisma (continua 6.19.3 — migrar pra 7/8 é decisão separada).

## ⚠️ Problemas encontrados e soluções
- Ver seção "Auditoria de segurança" no `progress.md` — lista completa dos 6 achados +
  fixes + testes (todos confirmados em produção via curl).
- `semgrep login` não funciona no terminal não-interativo do Claude Code (`! comando`) —
  precisou gerar token manual no site (`SEMGREP_APP_TOKEN`) em vez do fluxo OAuth do CLI.

## 🔧 Configurações adicionadas
- `overrides.deepmerge-ts` no `package.json`.
- `src/lib/ratelimit.ts`: nova ação `"clarify"`.
- `src/lib/security.ts`: helper `safeEqual` (constant-time compare).
- Headers de segurança + CSP em `next.config.ts`.
- `.semgrep-scan/` no `.gitignore` (output local do scan, não commitado).

## 📊 Estado atual do projeto
- Produto: Fases 1-7 completas e no ar (`cotor-ia.vercel.app`), Galeria 45/45.
- Segurança: 6 achados da auditoria manual corrigidos e testados; scan determinístico
  (Semgrep Pro + Trail of Bits) confirmou 0 findings. `npm audit` limpo.

## 🚧 Próxima sessão (To-do)
- ⚠️ **Revogar o token do Semgrep** (`semgrep.dev` → Settings → Tokens) — foi colado em
  texto puro no chat durante a sessão, gerar um novo se for escanear de novo.
- Testar troca de plano pela UI (Starter↔Pro).
- E-mails do COTOR (welcome/renovação/falha/cancelamento) — hoje só o Asaas manda.
- Histórico de faturas em `/app/conta` · aviso antes do 429 · plano Team de verdade ·
  pastas (`Project`) na biblioteca (schema existe, sem UI, sem rota — confirmado que não é
  superfície de ataque, só feature não construída).

## 💡 Observações importantes
- `owasp-security-check` e `trailofbits/skills@semgrep` agora instaladas globalmente —
  disponíveis por padrão em qualquer projeto que o Marcelo pedir auditoria de segurança.
- Semgrep Pro é grátis até 10 repos privados/10 contribuidores — vale reusar em outros
  projetos do Marcelo sem custo.
