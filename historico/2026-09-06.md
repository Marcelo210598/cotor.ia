# Snapshot - 2026-09-06

## 🎯 O que foi feito hoje
- Fechada a pendência crítica nº1: **login Google em produção**.
  - Registrado no Google Cloud (client OAuth Web `cotor-ia-local`, projeto `cotor-ia`):
    - Origem JavaScript autorizada: `https://cotor-ia.vercel.app`
    - URI de redirecionamento autorizado: `https://cotor-ia.vercel.app/api/auth/callback/google`
    - (mantido o `http://localhost:3000/api/auth/callback/google`)
  - Testado end-to-end pela UI em produção (Claude dirigiu o Chrome):
    `/entrar` → "Continuar com o Google" → account chooser "Prosseguir para
    cotor-ia.vercel.app" → consentimento → cai em `/app` logado como
    `difoggijuniormarcelo@gmail.com`. Sem `redirect_uri_mismatch`.
  - Persistência conferida no Neon: `users:1, sessions:2, accounts:1` — o login
    de prod reusou o mesmo `user` (mesmo Google sub do teste local de ontem) e
    criou uma nova `session`.
- **OAuth consent screen publicada** — status "Em produção" (não mais "Teste").
  - Branding completado: nome `cotor.ia`, e-mail de suporte + contato do dev,
    domínio autorizado `cotor-ia.vercel.app` (aceito), links:
    home `https://cotor-ia.vercel.app`, privacidade `.../privacidade`,
    termos `.../termos`.
  - Escopos só `email profile openid` (não-sensíveis) → **Google não exige
    verificação**. Qualquer conta Google já consegue logar (adeus limite de
    100 test users). Rollback disponível: botão "Voltar para o teste".

## 📝 Decisões técnicas importantes
- `NEXT_PUBLIC_APP_URL` no Vercel (Production) já estava correto
  (`https://cotor-ia.vercel.app`) — o `baseURL` do Better Auth em `src/lib/auth.ts`
  resolve certo em prod, então não precisou mexer em env var.
- Projeto local linkado ao Vercel (`vercel link` → `cotor-ia`,
  team `marcelo-di-foggia-juniors-projects`). `.vercel/` e `.env.local` já no
  `.gitignore` — tree continua limpo.

## ⚠️ Problemas encontrados e soluções
- Nenhum. Propagação do Google Cloud foi praticamente instantânea.

## 🔧 Configurações adicionadas
- Google Cloud OAuth client: +1 origem JS, +1 redirect URI (produção).

## 🎯 Parte 2 do dia — Fase 4a + 4b (Organize)
- **Layout compartilhado** `src/app/app/layout.tsx` (auth-gate + header com nav
  Novo/Biblioteca via `app-nav.tsx` + footer); `/app/page.tsx` enxugado pro composer.
- **Biblioteca** `/app/prompts` (`page.tsx` server + `library-client.tsx`):
  lista, busca client-side (título+intenção), filtro por tipo e por tag, menu ⋯
  (arquivar/desarquivar + excluir com dialog). Query em `src/lib/prompts/queries.ts`.
- **Detalhe** `/app/prompts/[id]` (`prompt-detail.tsx`): trilha de versões,
  visão da versão via `PromptResult`, **comparar** (`version-diff.tsx` — diff
  campo-a-campo do IR + delta de nota), **restaurar** (nova versão BRANCH,
  copia o score da origem: texto idêntico = nota determinística, zero LLM),
  editar título/tags inline.
- APIs novas: `PATCH`/`DELETE /api/prompts/[id]`, `POST /api/prompts/[id]/restore`.
- `TASK_TYPE_LABELS` em `schema.ts`. Composer ganhou link "Na biblioteca".
- **Sem migration** (schema já tinha `tags`, `archived`, `parentId`). Build + lint limpos.
- Testado no navegador (localhost:3000, dev do Marcelo + hot-reload): lista,
  filtros, detalhe, compare, restore, arquivar/desarquivar — tudo ok. v3 de
  teste do restore removida do Neon.

## 🔧 Parte 3 — ajustes de qualidade
- **"com" vs "de" no motor de imagem:** "foto COM o neymar" era lido como retrato
  solo do Neymar (+ "sem múltiplas pessoas" no negative — o oposto do pedido).
  Regras novas em `prompts.ts`: comitativa ("foto com X", "eu e X", "ao lado de")
  → pergunta quem mais aparece + foto de referência + sinaliza img2img p/ pessoa
  real. `PROMPT_VERSION` = `2026-09-06.1`. Testado no navegador, agora pergunta.
- **Bug: prompt duplicado na biblioteca.** A clarificação criava um Prompt e a
  síntese criava outro. Fix: clarificação stateless (só perguntas), Prompt criado
  uma vez só na síntese + `ClarificationSession` junto. `route.ts` (removido o
  branch `existingId` do persist e o `promptId` do bodySchema) + `composer.tsx`.
  Testado: "quero uma foto minha com o neymar" → clarify → pular → total de
  prompts 3→4 (não 3→5). Prompt de teste removido do Neon.

## 🔧 Parte 4 — guardrail não vaza mais pro prompt de imagem
- Sintoma: prompt gerado tinha "Nota técnica: usuário fornecerá foto de
  referência…" no meio do texto que vai pro modelo de imagem. Gemini recusou
  (Flash-Lite = sem geração de imagem; e retrato de pessoa pública real é
  bloqueado de qualquer forma).
- Fix: pra IMAGE, `guardrails` = canal só de aviso (não entra no `renderImage`).
  `PromptResult` recebe `taskType` e mostra bloco "⚠️ Antes de usar" com os
  guardrails. `prompts.ts`: síntese IMAGE proíbe meta-nota em objetivo/contexto
  e define 2 avisos padrão (img2img p/ pessoa real; geradores mainstream recusam
  pessoa pública). `PROMPT_VERSION` = `2026-09-06.2`.
- Testado: "quero uma foto minha com o neymar numa balada" → prompt limpo (só
  descrição visual) + callout com os 3 avisos. Dedup reconfirmado (caminho com
  respostas: 1 prompt).
- **Validação end-to-end pelo Marcelo:** "foto com neymar" → respondeu (Eu / sim /
  balada / realista) → prompt limpo score 73 → colou no Gemini + foto de
  referência dele → **Gemini gerou a imagem** (ele + Neymar numa balada, rosto
  dele preservado). Antes o Gemini recusava por causa da "Nota técnica" no corpo
  do prompt. Fluxo COTOR → gerador de imagem funcionando de ponta a ponta.

## 📊 Estado atual do projeto
- Fases 1, 2, 3 ✅ + **Fase 4a/4b ✅** + 3 ajustes de motor. Deploy no ar, login
  prod ok. ~60% do roadmap.

## 🚧 Próxima sessão (To-do)
- **Fase 4c — Reuse:** variáveis `{{x}}` + templates (provável modelo `Template`
  novo → migration).
- **Fase 5 — Playground + Billing (Asaas) + rate limit (Upstash).**
- Trocar o glifo losango do `MadeBy` pela logo real do marcelo.dev quando chegar.

## 💡 Observações importantes
- OAuth consent screen já **publicada** ("Em produção") — sem verificação porque
  os escopos são básicos. Se um dia adicionar escopo sensível/restrito (Drive,
  Gmail, etc.) aí sim cai em verificação do Google + tela "app não verificado".
- Como o domínio é `vercel.app` (subdomínio compartilhado), o Google aceitou
  como domínio autorizado nesse caso. Quando migrar pra domínio próprio, atualizar
  domínio autorizado + os 3 links no Branding + os redirect URIs no client.

## 🧩 Fase 4c — Reuse (templates + variáveis)
- Modelo `Template` (name/description/body/variables[]/sourcePromptId), `db push` no Neon.
- `templatize` (Haiku) em `engine.ts` + prompt conservador em `prompts.ts`
  (`PROMPT_VERSION` = `2026-09-06.4`). 1ª versão do prompt templatizava demais
  (transformou os blocos de EXEMPLO do prompt de notas fiscais em 24 variáveis) —
  reescrito pra: 0–6 variáveis, nunca tocar em few-shot, não parametrizar input colado depois.
- APIs `/api/templates` (list/create), `/api/templates/[id]` (patch/delete),
  `/api/templates/templatize`. Queries em `src/lib/templates/queries.ts`, utils em `vars.ts`.
- Páginas: `/app/templates` (lista) + `/app/templates/[id]` (preencher → preview ao
  vivo → copiar, + modo editar). Dialog "Salvar como template" no `/app/prompts/[id]`.
- **Bug do dialog:** `DialogContent` sem `max-height` → textarea com o prompt
  inteiro estourava o modal pra fora da viewport (título e inputs off-screen).
  Fix em `dialog.tsx` (`max-h-[calc(100dvh-2rem)] overflow-y-auto`) + cap nos textareas.
- Testado E2E: prompt "email de cobrança pro Ricardo Souza" → templatizar → 4
  variáveis corretas → preencher (Joana Pereira / 8829 / R$ 1.480 / 22/03) →
  preview certo → copiar. Dados de teste removidos do Neon.
- ⚠️ Rodei `npm run build` com o dev server do Marcelo ligado (compartilham `.next`) —
  se o dev ficar estranho, reiniciar.

## 📊 Estado (fim do dia)
- **Fases 1, 2, 3, 4 ✅.** Deploy no ar, login prod ok. ~70% do roadmap.
- Próximo: Fase 5 (Playground + Asaas + Upstash), logo marcelo.dev.

## 🎮 Fase 5a + 5b — Playground + Rate limit
- **Playground:** `runPrompt` em `engine.ts`, rota `/api/playground`, componente
  `PlaygroundPanel` (Groq "Rápido" / Claude Haiku, saída scrollável). No composer,
  `/app/prompts/[id]` e `/app/templates/[id]`. IMAGE desabilitado.
- **Rate limit:** `src/lib/ratelimit.ts` (`@upstash/ratelimit`+`@upstash/redis`),
  1 dia deslizante, por plano (FREE 20/dia gerações). Em cotor(síntese)/optimize/
  templatize/playground. 429 amigável. **Fallback:** sem env Upstash = não limita.
- `auth.ts`: `user.additionalFields.plan` → `session.user.plan`.
- Testado: playground OK (Groq respondeu ao prompt de NF-e), IMAGE mostra
  "cola direto no Midjourney".
- 🔴 Falta: Marcelo criar o Redis no Upstash e passar URL+TOKEN.
- 3 high vulns no `npm audit` são pré-existentes (deepmerge-ts, "fix" quer
  downgrade do Prisma) — não é do install do Upstash.

## 📊 Estado (fim do dia, atualizado)
- **Fases 1–4 ✅ + 5a/5b ✅** (falta plugar Upstash + Fase 5c Asaas). ~80% do roadmap.

## ✅ Upstash plugado (fim da Fase 5b)
- Redis `cotor.ia` no Upstash (Free, AWS sa-east-1). Host
  `robust-mongoose-115424.upstash.io`. Env `UPSTASH_REDIS_REST_URL` + `_TOKEN`
  no `.env` local + Vercel Production (via `vercel env add` CLI, não deu pra
  usar a UI da Vercel — mudou muito).
- **Descoberta:** o token do `redis-cli`/TCP é o MESMO do `UPSTASH_REDIS_REST_TOKEN`
  (testei com `curl .../ping` → PONG).
- Rate limit **ativo em produção**. Comprovado end-to-end (script node: limite 2,
  3ª chamada `success:false`). Redeploy `cotor-jbnxqpfu8`.
