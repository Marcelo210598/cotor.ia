# Snapshot - 2026-09-11

## 🎯 O que foi feito hoje
- Retomei a pendência da Fase 7 (Galeria): seed estava em 31/45 desde 07/09.
- Rodei `FRESH=1 npx tsx --env-file=.env scripts/seed-gallery.ts` (recria os
  45 do zero) — foi até **39/45** e travou em rate limit do Groq.
- Rodei o gap-fill (mesmo comando, sem `FRESH=1`) mais 2x, com espera de
  minutos entre as tentativas — sempre travou nos mesmos 6 itens.

## 📝 Decisões técnicas importantes
- Confirmado que o gap-fill do script é idempotente por `title`: sem
  `FRESH=1` ele só semeia o que não existe ainda, não mexe nos que já deram
  certo. Rodar sem `FRESH=1` é seguro pra tentar de novo.

## ⚠️ Problemas encontrados e soluções
- Problema: seed trava sempre nos mesmos 6 itens ("Parceiro de brainstorm" +
  os 5 de AGENT), erro 429 do Groq (`tokens per day (TPD): Limit 200000`).
- Causa: o TPD do Groq free **é compartilhado com o app em produção** — o
  COTOR.IA usa o mesmo `GROQ_API_KEY` pra gerar prompt de usuário real, então
  a cota já vem consumida pelo tráfego do dia e não esvazia rápido.
- Não resolvido ainda. Esperar poucos minutos não adianta (a cota volta a
  encher com uso real quase na hora). Próxima tentativa deveria ser de
  madrugada, ou considerar Groq Dev Tier (pago) se isso virar recorrente.

## 🔧 Configurações adicionadas
- Nenhuma (só uso de infra existente).

## 📊 Estado atual do projeto
- Galeria (Fase 7): **39/45 prompts**. Faltam AGENT (0/5) + 1 CONVERSATION
  ("Parceiro de brainstorm"). Todo o resto do produto segue como em 07/09
  (Fases 1-6 no ar, billing Asaas em produção).

## 🚧 Próxima sessão (To-do)
- Rodar `npx tsx --env-file=.env scripts/seed-gallery.ts` **sem** `FRESH=1`
  de madrugada (baixo tráfego real) pra fechar os 6 que faltam.
- Se continuar travando: considerar Groq Dev Tier pago, ou reservar uma cota
  separada pro seed (chave Groq própria, não a de produção).

## 💡 Observações importantes
- Ver `historico/resumo.md` e `progress.md` (topo) — atualizados com esse
  achado do TPD compartilhado, pra não repetir a tentativa "esperar uns
  minutos" que já não funcionou 3x.
- Revisei o `progress.md` inteiro pra listar tudo que falta pro app (fora a
  Galeria). Achei uma pendência desatualizada: "logo real do marcelo.dev" já
  tinha sido feita em 07/09 (glifo virou diamantes concêntricos + gradiente,
  `src/components/brand/made-by.tsx`) mas continuava listada como pendente —
  corrigido.
- **Pendências reais que restam** (fora a Galeria, que fica pra depois):
  - Billing: e-mails próprios do COTOR (welcome/renovação/falha/cancelamento,
    hoje só o Asaas manda); histórico de faturas em `/app/conta`; aviso
    "usou X de Y" antes do 429; plano anual/desconto; plano Team de verdade
    (hoje só CTA na landing).
  - Produto: pastas (`Project`) na biblioteca — schema existe, sem UI.
  - Deploy: domínio próprio (SEO/GEO e página pública já prontos, falta só o
    domínio em si — decisão de adiar segue de pé).
  - Menores/opcionais: `unstable_cache` no `/p/[id]`; `SCORE_SAMPLES=3`
    quando o Groq for pago.
